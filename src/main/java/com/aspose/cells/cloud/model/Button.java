package com.aspose.cells.cloud.model;

import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;

/** Button — Represents the Forms control: Button */
@JsonInclude(JsonInclude.Include.NON_NULL)
public class Button extends Shape {
    /** Gets or sets link. */
    @JsonProperty("link")
    private Link link;

    public Link getLink() { return link; }
    public Button setLink(Link link) { this.link = link; return this; }


    /** Covariant override: keeps fluent chains returning Button. */
    @Override public Button setName(String name) { return (Button) super.setName(name); }
    /** Covariant override: keeps fluent chains returning Button. */
    @Override public Button setMsoDrawingType(String msoDrawingType) { return (Button) super.setMsoDrawingType(msoDrawingType); }
    /** Covariant override: keeps fluent chains returning Button. */
    @Override public Button setAutoShapeType(String autoShapeType) { return (Button) super.setAutoShapeType(autoShapeType); }
    /** Covariant override: keeps fluent chains returning Button. */
    @Override public Button setPlacement(String placement) { return (Button) super.setPlacement(placement); }
    /** Covariant override: keeps fluent chains returning Button. */
    @Override public Button setUpperLeftRow(Integer upperLeftRow) { return (Button) super.setUpperLeftRow(upperLeftRow); }
    /** Covariant override: keeps fluent chains returning Button. */
    @Override public Button setTop(Integer top) { return (Button) super.setTop(top); }
    /** Covariant override: keeps fluent chains returning Button. */
    @Override public Button setUpperLeftColumn(Integer upperLeftColumn) { return (Button) super.setUpperLeftColumn(upperLeftColumn); }
    /** Covariant override: keeps fluent chains returning Button. */
    @Override public Button setLeft(Integer left) { return (Button) super.setLeft(left); }
    /** Covariant override: keeps fluent chains returning Button. */
    @Override public Button setLowerRightRow(Integer lowerRightRow) { return (Button) super.setLowerRightRow(lowerRightRow); }
    /** Covariant override: keeps fluent chains returning Button. */
    @Override public Button setBottom(Integer bottom) { return (Button) super.setBottom(bottom); }
    /** Covariant override: keeps fluent chains returning Button. */
    @Override public Button setLowerRightColumn(Integer lowerRightColumn) { return (Button) super.setLowerRightColumn(lowerRightColumn); }
    /** Covariant override: keeps fluent chains returning Button. */
    @Override public Button setRight(Integer right) { return (Button) super.setRight(right); }
    /** Covariant override: keeps fluent chains returning Button. */
    @Override public Button setWidth(Integer width) { return (Button) super.setWidth(width); }
    /** Covariant override: keeps fluent chains returning Button. */
    @Override public Button setHeight(Integer height) { return (Button) super.setHeight(height); }
    /** Covariant override: keeps fluent chains returning Button. */
    @Override public Button setX(Integer x) { return (Button) super.setX(x); }
    /** Covariant override: keeps fluent chains returning Button. */
    @Override public Button setY(Integer y) { return (Button) super.setY(y); }
    /** Covariant override: keeps fluent chains returning Button. */
    @Override public Button setRotationAngle(Double rotationAngle) { return (Button) super.setRotationAngle(rotationAngle); }
    /** Covariant override: keeps fluent chains returning Button. */
    @Override public Button setHtmlText(String htmlText) { return (Button) super.setHtmlText(htmlText); }
    /** Covariant override: keeps fluent chains returning Button. */
    @Override public Button setText(String text) { return (Button) super.setText(text); }
    /** Covariant override: keeps fluent chains returning Button. */
    @Override public Button setAlternativeText(String alternativeText) { return (Button) super.setAlternativeText(alternativeText); }
    /** Covariant override: keeps fluent chains returning Button. */
    @Override public Button setTextHorizontalAlignment(String textHorizontalAlignment) { return (Button) super.setTextHorizontalAlignment(textHorizontalAlignment); }
    /** Covariant override: keeps fluent chains returning Button. */
    @Override public Button setTextHorizontalOverflow(String textHorizontalOverflow) { return (Button) super.setTextHorizontalOverflow(textHorizontalOverflow); }
    /** Covariant override: keeps fluent chains returning Button. */
    @Override public Button setTextOrientationType(String textOrientationType) { return (Button) super.setTextOrientationType(textOrientationType); }
    /** Covariant override: keeps fluent chains returning Button. */
    @Override public Button setTextVerticalAlignment(String textVerticalAlignment) { return (Button) super.setTextVerticalAlignment(textVerticalAlignment); }
    /** Covariant override: keeps fluent chains returning Button. */
    @Override public Button setTextVerticalOverflow(String textVerticalOverflow) { return (Button) super.setTextVerticalOverflow(textVerticalOverflow); }
    /** Covariant override: keeps fluent chains returning Button. */
    @Override public Button setIsGroup(Boolean isGroup) { return (Button) super.setIsGroup(isGroup); }
    /** Covariant override: keeps fluent chains returning Button. */
    @Override public Button setIsHidden(Boolean isHidden) { return (Button) super.setIsHidden(isHidden); }
    /** Covariant override: keeps fluent chains returning Button. */
    @Override public Button setIsLockAspectRatio(Boolean isLockAspectRatio) { return (Button) super.setIsLockAspectRatio(isLockAspectRatio); }
    /** Covariant override: keeps fluent chains returning Button. */
    @Override public Button setIsLocked(Boolean isLocked) { return (Button) super.setIsLocked(isLocked); }
    /** Covariant override: keeps fluent chains returning Button. */
    @Override public Button setIsPrintable(Boolean isPrintable) { return (Button) super.setIsPrintable(isPrintable); }
    /** Covariant override: keeps fluent chains returning Button. */
    @Override public Button setIsTextWrapped(Boolean isTextWrapped) { return (Button) super.setIsTextWrapped(isTextWrapped); }
    /** Covariant override: keeps fluent chains returning Button. */
    @Override public Button setIsWordArt(Boolean isWordArt) { return (Button) super.setIsWordArt(isWordArt); }
    /** Covariant override: keeps fluent chains returning Button. */
    @Override public Button setLinkedCell(String linkedCell) { return (Button) super.setLinkedCell(linkedCell); }
    /** Covariant override: keeps fluent chains returning Button. */
    @Override public Button setZOrderPosition(Integer zOrderPosition) { return (Button) super.setZOrderPosition(zOrderPosition); }
    /** Covariant override: keeps fluent chains returning Button. */
    @Override public Button setFont(Font font) { return (Button) super.setFont(font); }
    /** Covariant override: keeps fluent chains returning Button. */
    @Override public Button setHyperlink(String hyperlink) { return (Button) super.setHyperlink(hyperlink); }

}
